import ArgumentParser

struct Doctor: ParsableCommand {
    mutating func run() throws {
        /// `TODO`
        /// - Check `which oj`
        /// - Check `which oj-api`
        print("TBD")
    }
}
